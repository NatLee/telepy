/**
 * API 行為與回應相關型別 / API behavior and response types
 * Defines the shapes for common API responses.
 * PaginatedResponse / ApiErrorResponse 已移除（未使用）；若未來需分頁或標準錯誤格式可在此補回。
 * PaginatedResponse / ApiErrorResponse removed (unused); re-add here when pagination or standard error typing is needed.
 */
