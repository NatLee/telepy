# =========[ General Settings ]=========
PROJECT_NAME=main
DEBUG=false

# =========[ Server Ports ]=========
WEB_SERVER_PORT=8787
REVERSE_SERVER_SSH_PORT=24242

# =========[ OAuth / Social Login ]=========
SOCIAL_GOOGLE_CLIENT_ID=376808175534-d6mefo6b1kqih3grjjose2euree2g3cs.apps.googleusercontent.com

# =========[ Domain / Server ]=========
SERVER_DOMAIN=localhost

# =========[ Internal / Secrets ]=========
INTERNAL_API_TOKEN=your-random-secret-string

# =========[ Proxy Browser (CDP) ]=========
# 遠端瀏覽器改用共用 headless Chromium + CDP screencast,畫面走既有 /ws,不再需要
# WebRTC 的 UDP 埠段或 NAT1TO1 設定(只要能開網頁就能用,含只開 443 / 對稱 NAT)。
# 進階畫質/session 參數在 SiteSettings(admin)內調整,無需在此設定。
# Remote browser now uses a shared headless Chromium + CDP screencast over the existing
# /ws — no WebRTC UDP port range or NAT1TO1 needed. Tune quality/session limits in SiteSettings.